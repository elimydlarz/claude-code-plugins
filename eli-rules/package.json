{
  "name": "@susu-eng/eli-rules",
  "version": "0.1.0",
  "description": "Reusable Claude Code rules for coding principles and practices",
  "type": "module",
  "bin": {
    "eli-rules-sync": "./sync.mjs"
  },
  "scripts": {
    "build": "echo 'No build step required'",
    "test": "vitest run",
    "postinstall": "node sync.mjs",
    "prepublishOnly": "pnpm build"
  },
  "files": [
    "sync.mjs",
    "rules/"
  ],
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "claude",
    "claude-code",
    "rules"
  ],
  "license": "MIT",
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
