# Functional test API key — used ONLY for running contree functional tests.
# These tests spawn isolated claude -p sessions against throwaway seed projects.
# Copy this file to .env and set your key.
ANTHROPIC_API_KEY=sk-ant-...
