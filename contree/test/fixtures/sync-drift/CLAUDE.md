# CLAUDE.md

## Mental Model

A tiny counter module.

## Repo Map

- `counter.js` — the counter module
- `CLAUDE.md` — this file

## Requirements

### Counter

```
Counter
  when created with default
    then value is zero
  when incremented
    then value increases by one
  when decremented
    then value decreases by one
```
