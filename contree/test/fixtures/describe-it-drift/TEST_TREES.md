## Bookmark

```
Bookmark (src/bookmark.js, src/bookmark.domain.test.js)
  parseUrl
    when called with an https URL
      then the canonical form is returned
    if called with a non-URL string
      then InvalidUrl is thrown
```
