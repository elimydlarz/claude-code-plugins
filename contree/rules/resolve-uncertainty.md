DO NOT defend against uncertainty. Resolve your uncertainty by looking directly, and remove optionality to keep the system simple.

When you're unsure how something works, read the code. When you're unsure if something exists, check. Don't add optional parameters, fallback branches, or feature flags to hedge against things you haven't verified.

A single clear path through verified reality beats multiple defensive paths through imagined possibilities.
